<VirtualHost *:80 [::]:80>
    ServerAdmin webmaster@localhost
    DocumentRoot /app/envyscuola_it

    # Accetta tutte le richieste di nomi di dominio
    ServerName envyscuola.it
    ServerAlias www.envyscuola.it

    <Directory /app>
        Options Indexes FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>

    # Gestione degli errori
    ErrorLog /log/error_envyscuola_it.log
    CustomLog /log/access_envyscuola_it.log combined

    # Abilita la compressione (opzionale ma consigliata per prestazioni migliori)
    <IfModule mod_deflate.c>
        AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css application/javascript application/json
    </IfModule>
</VirtualHost>
